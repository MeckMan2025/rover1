# Start patrolling a saved path
string path_name

# Loop options: 0 = infinite, >0 = specific count
int32 loop_count

# Reverse mode: traverse forward then backward
bool reverse_mode

# Speed multiplier (0.0-1.0, default 1.0)
float32 speed_percent
---
bool success
string message
